{
  "name": "@poppy/conformance",
  "version": "0.1.0-alpha.0",
  "description": "Cross-platform test corpus and runner harness for Poppy renderers.",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/greg7gkb/poppy.git",
    "directory": "packages/conformance"
  },
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    },
    "./cases/*": "./cases/*"
  },
  "files": ["src/", "cases/", "README.md"],
  "scripts": {
    "build": "echo '@poppy/conformance is a source-only package in v0.1; no build step.'",
    "typecheck": "tsc --noEmit",
    "test": "echo '@poppy/conformance has no own tests; consumed by renderer packages'"
  },
  "devDependencies": {
    "@types/jsdom": "^21.1.7",
    "@types/node": "^22.0.0",
    "jsdom": "^25.0.1",
    "typescript": "^5.6.0"
  }
}
